"""Assertion extraction — pure function library.

No dependency on `intake/` or `loop/`. The dispatcher imports
this module as a pure function library. State mutation (JSONL append)
is the caller's responsibility.
"""
