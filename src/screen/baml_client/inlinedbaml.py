# ----------------------------------------------------------------------------
#
#  Welcome to Baml! To use this generated code, please run the following:
#
#  $ pip install baml
#
# ----------------------------------------------------------------------------

# This file was generated by BAML: please do not edit it. Instead, edit the
# BAML files and re-generate this code using: baml-cli generate
# baml-cli is available with the baml package.

_file_map = {

    "clients.baml": "// Three LLM clients configured per `.env`. BAML names are PascalCase\n// keywords; switching happens via `client \"Fireworks\"|\"Openai\"|\"Anthropic\"`\n// in `identify.baml`. To swap at runtime by env var use BAML's\n// `ClientRegistry`.\n\nclient<llm> Openai {\n  provider openai\n  options {\n    model \"gpt-4.1-mini\"\n    api_key env.OPENAI_API_KEY\n  }\n}\n\nclient<llm> Fireworks {\n  provider \"openai-generic\"\n  options {\n    base_url \"https://api.fireworks.ai/inference/v1\"\n    model \"accounts/fireworks/models/nemotron-3-ultra-nvfp4\"\n    api_key env.FIREWORKS_API_KEY\n  }\n}\n\nclient<llm> Anthropic {\n  provider anthropic\n  options {\n    model \"claude-haiku-4-5-20251001\"\n    api_key env.ANTHROPIC_API_KEY\n  }\n}\n\n// Generates `src/screen/baml_client/` from these definitions. Run\n// `baml-cli generate` (or use the BAML VSCode extension's\n// save-on-edit hook) to regenerate.\ngenerator target {\n  output_type python/pydantic\n  output_dir \"..\"\n  version \"0.226.1\"\n}\n",
    "digest.baml": "// Generate a short prose gist of a dimension's assertion mix.\n//\n// Never a verdict, count, or Fit word (Poor/Mixed/Strong) — the digest\n// summarizes what the evidence says, in the voice of someone who read it,\n// not that a summary exists (\"there are 3 assertions...\" is banned).\n// Output is validated against `screen.digest.style.assert_valid_digest`\n// before it is cached — this prompt is a bias toward that shape, not the\n// enforcement point.\n\nfunction DigestDimension(\n  assertions_json: string,\n  rubric_text: string,\n) -> string {\n  client Fireworks\n  prompt #\"\n    You are a job-screener research assistant. Read the assertions below,\n    all filed against one rubric dimension for one opening, and write a\n    short, scannable digest summarizing what the evidence actually says.\n\n    ## Rubric context\n    {{ rubric_text }}\n\n    ## Style\n    - 30 words or fewer, total.\n    - Plain prose by default. Use bullets (each line starting with \"- \")\n      only when the evidence splits into distinct facets or a genuine\n      tension — never to pad a single point into a list.\n    - Short phrases and fragments are fine if they scan faster than a\n      full sentence.\n\n    ## Rules\n    - Never use the words Poor, Mixed, or Strong.\n    - Never state a number or count of assertions.\n    - Never say that you are summarizing, or that assertions/evidence\n      \"exist\" or \"show\" something — write as someone who read the\n      evidence and is describing what it says, directly.\n    - If the assertions conflict, say what the tension is, in plain terms.\n    - If the list is empty, say the dimension has not yet been examined.\n\n    ## Assertions (JSON)\n    {{ assertions_json }}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Smoke test: verify DigestDimension holds the word budget, drops em-dashes,\n// and produces prose without a Fit word. Run with `baml-cli test` (not\n// `scripts/check.py`). Costs one model call.\ntest DigestDimension_HighBarCulture {\n  functions [DigestDimension]\n  args {\n    assertions_json #\"\n[{\"target\": \"internal_culture\", \"fit\": \"Strong\", \"chunk\": \"We are not a place for complacency.\", \"citations\": [{\"url\": \"https://example.com/jobs/1\", \"quote\": \"We are not a place for complacency.\", \"host\": \"example.com\", \"source_provenance\": \"official\", \"independent\": true, \"source_date\": null}]}]\n    \"#\n    rubric_text #\"\nConstraint slug: internal_culture. Fit: Poor = negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n  }\n  @@assert(non_empty, {{ this|length > 0 }})\n  @@assert(word_budget, {{ this.split() | length <= 30 }})\n  @@assert(no_em_dash, {{ \"—\" not in this }})\n}\n\n// Smoke test: two assertions with a genuine tension should be allowed (not\n// required) to render as bullets, and must still hold the word budget.\ntest DigestDimension_ConflictingEvidence {\n  functions [DigestDimension]\n  args {\n    assertions_json #\"\n[{\"target\": \"stretch\", \"fit\": \"Strong\", \"chunk\": \"Engineers own features end to end with real autonomy.\", \"citations\": [{\"url\": \"https://example.com/jobs/1\", \"quote\": \"Engineers own features end to end.\", \"host\": \"example.com\", \"source_provenance\": \"official\", \"independent\": true, \"source_date\": null}]}, {\"target\": \"stretch\", \"fit\": \"Poor\", \"chunk\": \"Recent reviews describe heavy process and slow approvals.\", \"citations\": [{\"url\": \"https://example.com/reviews/2\", \"quote\": \"Heavy process, slow approvals.\", \"host\": \"example.com\", \"source_provenance\": \"third_party\", \"independent\": true, \"source_date\": null}]}]\n    \"#\n    rubric_text #\"\nConstraint slug: stretch. Fit: Poor = negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n  }\n  @@assert(non_empty, {{ this|length > 0 }})\n  @@assert(word_budget, {{ this.split() | length <= 30 }})\n  @@assert(no_em_dash, {{ \"—\" not in this }})\n}\n",
    "extract.baml": "// Extract typed assertions from one page-content chunk.\n//\n// The output schema matches `screen.types.Assertion` field-for-field.\n// `target` must be one of the closed vocabulary slugs listed in the\n// rubric_text argument; `fit` must be Poor, Mixed, or Strong.\n// `citations` is always non-empty — every assertion must be grounded.\n//\n// The function receives `existing_assertions` so it can avoid\n// duplicating claims already extracted in a prior pass.\n\nclass Citation {\n  url string @description(\"The URL of the source page where this claim was found.\")\n  quote string @description(\"The verbatim span from the source that supports this claim. Must be present and non-empty.\")\n  host string @description(\"The domain serving the content, e.g. 'linkedin.com' or 'acme.com'.\")\n  source_provenance string @description(\"Whether this host is the originating source or an aggregator mirror, e.g. 'official', 'aggregator', 'news'.\")\n  independent bool @description(\"True if this citation is corroborating evidence from an independent source; false if it is the same underlying claim re-published.\")\n  source_date string? @description(\"ISO date (YYYY-MM-DD) when the claim was observed, if available. Null if unknown.\")\n}\n\nclass Assertion {\n  target string @description(\"The rubric slug this claim pertains to. Must be one of the scoring dimension slugs, constraint slugs, or 'non_scoring:obtainability'. See the rubric text for the complete closed vocabulary.\")\n  fit string @description(\"Poor, Mixed, or Strong — the claim's polarity in rubric vocabulary. Do not emit numeric scores.\")\n  provenance string @description(\"Always 'model_proposed' for assertions produced by this function.\")\n  chunk string @description(\"The verbatim span from the page content that grounded this claim. Must be non-empty.\")\n  citations Citation[] @description(\"One or more citations. Always non-empty. For a job posting the URL is the posting URL and the quote is the span supporting the claim.\")\n  created_at string @description(\"ISO 8601 UTC timestamp of extraction, e.g. '2026-08-26T12:00:00Z'.\")\n}\n\nfunction ExtractAssertions(\n  chunk: string,\n  rubric_text: string,\n  existing_assertions: string,\n) -> Assertion[] {\n  client Fireworks\n  prompt #\"\n    You are a job-screener research assistant. Read the page content below\n    and extract typed, source-cited assertions about the company or opening.\n\n    ## Rubric\n    {{ rubric_text }}\n\n    ## Rules\n    - Each assertion must have a `target` from the rubric's closed slug vocabulary\n      (dimension slugs, constraint slugs, or `non_scoring:obtainability`).\n    - `fit` must be exactly one of: Poor, Mixed, Strong.\n    - `provenance` must be `model_proposed`.\n    - `chunk` must be a verbatim span copied from the page content below.\n    - `citations` must be non-empty. Use the page URL as the citation URL\n      and a verbatim span as the quote.\n    - Do NOT emit numeric scores or confidence weights.\n    - Do NOT duplicate claims already present in existing assertions.\n    - Produce only claims that are directly supported by the text below.\n      Do not infer or speculate.\n    - Aim for 3–8 assertions per chunk. More than 10 is a sign of over-extraction.\n\n    ## Existing assertions (do not duplicate)\n    {{ existing_assertions }}\n\n    ## Page content\n    ```\n    {{ chunk }}\n    ```\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Smoke test: verify ExtractAssertions extracts at least one typed assertion\n// from a realistic posting fixture.\n//\n// Fixture is the same anonymized Acme Health posting used in identify.baml.\n// Run with `baml-cli test` (not `scripts/check.py`). Costs one model call.\ntest ExtractAssertions_AcmeHealthStaffPlatform {\n  functions [ExtractAssertions]\n  args {\n    chunk #\"\n# Staff Platform Engineer\nAcme Health\n\nPosted 12 Days Ago · 3 Locations · In-Office or Remote · competitive · Senior level\n\nBuild and own the platform every engineer at Acme Health deploys on. We're a Series B company, well-funded, serving 200K users across the country. Our infrastructure runs on ECS/EKS, Terraform self-serve, autoscaling, and a Python monolith. We're hiring a Staff Platform Engineer to make infrastructure boring, scalable, and self-serve.\n\nAbout Us\n\nAcme Health is building a new scheduling system that providers run their practice on. The admin work — credentialing, claims, payment reconciliation — is automated end-to-end. We're a team of about 200 working across clinical operations, engineering, and platform. We've taken capital from Hypothetical Ventures and are growing the engineering team.\n\nAbout the Role\n\nYou'll lead Platform Engineering with peer managers across Application and AI. You'll own cloud platform architecture, deployment blast-radius containment, autoscaling trustworthiness, cost attribution, and Terraform self-serve. Our mandate is to make every change safe from the start and build systems that get safer as they get faster.\n\nAbout You\n\n10+ years in platform or infrastructure engineering. Shipped cloud platforms at staff level. Comfortable with Python, AWS, ECS/EKS, Terraform, fast-moving teams.\n    \"#\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    existing_assertions \"[]\"\n  }\n  @@assert({{ this|length > 0 }})\n  @@assert({{ this[0].chunk|length > 0 }})\n  @@assert({{ this[0].fit in [\"Poor\", \"Mixed\", \"Strong\"] }})\n  @@assert({{ this[0].citations|length > 0 }})\n}\n",
    "identify.baml": "// Identify Company + Opening from a posting's page content.\n//\n// The default client is Fireworks (cheap, capable, 8b). The function's\n// output fields map to `screen.types.IdentificationResult`; the\n// generated Pydantic in `src/screen/baml_client/` should match that\n// schema field-for-field, and the wrapper at\n// `screen.intake.identify` coerces the generated model into the\n// canonical one.\n\nclass IdentificationResult {\n  company_name string @description(\"The legal or commonly-known company name. Should not include department or product name.\")\n  opening_title string @description(\"The title of the role as it would appear to applicants (e.g., 'Staff Engineer' or 'Platform Lead').\")\n  opening_notes string @description(\"Free-text summary the LLM uses to distinguish openings when more than one is in scope. Not surfaced as a user-visible field.\")\n}\n\nfunction IdentifyOpening(page_content: string) -> IdentificationResult {\n  client Fireworks\n  prompt #\"\n    Read the following job posting and identify:\n\n    1. The company that posted the opening.\n    2. The title of the role.\n    3. A short free-text note distinguishing this role from another role\n       at the same company.\n\n    Page content:\n    ```\n    {{ page_content }}\n    ```\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Smoke-test: verify that IdentifyOpening extracts company name and\n// role title correctly from a realistic-shaped posting.\n//\n// Fixture anonymized from a real posting (data/b7a254636963): company\n// name → Acme Health, role → Staff Platform Engineer, recruiter\n// boilerplate (funding, sector text, salary) → neutral variants. Real\n// company names and figures do not appear here.\n//\n// Run with `baml-cli test` (not `scripts/check.py`). Costs one\n// model call per execution. Substring assertions absorb spelling drift\n// across model versions.\ntest IdentifyOpening_AcmeHealthStaffPlatform {\n  functions [IdentifyOpening]\n  args {\n    page_content #\"\n# Staff Platform Engineer\nAcme Health\n\nPosted 12 Days Ago · 3 Locations · In-Office or Remote · competitive · Senior level\n\nBuild and own the platform every engineer at Acme Health deploys on. We're a Series B company, well-funded, serving 200K users across the country. Our infrastructure runs on ECS/EKS, Terraform self-serve, autoscaling, and a Python monolith. We're hiring a Staff Platform Engineer to make infrastructure boring, scalable, and self-serve.\n\nAbout Us\n\nAcme Health is building a new schedulingcare system that providers run their practice on. The admin work — credentialing, claims, payment reconciliation — is automated end-to-end. We're a team of about 200 working across clinical operations, engineering, and platform. We've taken capital from Hypothetical Ventures and are growing the engineering team.\n\nAbout the Role\n\nYou'll lead Platform Engineering with peer managers across Application and AI. You'll own cloud platform architecture, deployment blast-radius containment, autoscaling trustworthiness, cost attribution, and Terraform self-serve. Our mandate is to make every change safe from the start and build systems that get safer as they get faster.\n\nAbout You\n\n10+ years in platform or infrastructure engineering. Shipped cloud platforms at staff level. Comfortable with Python, AWS, ECS/EKS, Terraform, fast-moving teams.\n    \"#\n  }\n  @@assert({{ \"Acme Health\" in this.company_name }})\n  @@assert({{ \"Staff\" in this.opening_title }})\n  @@assert({{ \"Platform\" in this.opening_title or \"Engineer\" in this.opening_title }})\n  @@assert({{ this.opening_notes|length > 0 }})\n}\n",
    "research.baml": "// DecidePlan — the one LLM-shaped decision in the research dispatch pass.\n//\n// Shape 1 invariant: BAML generates the plan; the hand-written\n// dispatcher owns every tool call and all state mutation.\n//\n// Returns a list of actions. StopAction, FetchAction, and SearchAction are\n// the supported tags. The output schema is a tagged union; the dispatcher\n// matches on `tag`.\n//\n// The prompt body does not restate the output schema in prose —\n// ctx.output_format handles it.\n\nclass StopAction {\n  tag string @description(\"Always 'stop'.\")\n  reason string @description(\"One sentence: why the pass is stopping. Must be non-empty.\")\n}\n\nclass FetchAction {\n  tag string @description(\"Always 'fetch'.\")\n  url string @description(\"The URL to fetch. Must be non-empty.\")\n}\n\nclass SearchAction {\n  tag string @description(\"Always 'search'.\")\n  query string @description(\"The search query to issue. Must be non-empty.\")\n}\n\n// Closed union for this slice.\ntype Action = StopAction | FetchAction | SearchAction\n\nfunction DecidePlan(\n  company_name: string,\n  opening_title: string,\n  rubric_text: string,\n  primary_target: string,\n  last_context_text: string,\n  prior_queries: string,\n) -> Action[] {\n  client Fireworks\n  prompt #\"\n    You are a research-pass planner for a job screener.\n\n    ## Your job\n    Decide what to do next in a research pass on a job opening.\n    Return a list of actions. Available actions: `stop`, `fetch`, `search`.\n\n    ## When to stop\n    Return `stop` only when the last action was a `fetch` that added\n    assertions for the primary target, and the fetched page has no\n    unexplored relevant links left to follow for the primary target.\n\n    Do not stop because the turn budget is low or exhausted — a separate\n    process ends the pass when the budget runs out; you never need to count\n    turns. Do not stop just because a search returned no immediately\n    fetchable URL — issue a different search instead. Do not stop because a\n    fetch added no assertions — that source was a dead end, try another one.\n\n    ## When to fetch\n    Fetch a specific URL when the last context revealed an outbound link worth\n    following to gather more evidence for the primary target.\n\n    Prefer links that directly address the primary target. Skip navigation\n    links, home/contact/login pages, social media, and generic \"about us\" links\n    — they rarely add scorable evidence. If the last context reveals only\n    low-value links, issue a search instead. If the last context reveals a\n    link that is relevant to the primary target but the page itself is likely\n    a hub, you may fetch another link from that page in a subsequent turn.\n\n    ## When to search\n    Issue a search query when there is no specific URL worth fetching from the\n    last context for the primary target. A search opens new sourcing paths — use\n    it to find evidence for the primary target (e.g. compensation benchmarks,\n    engineering culture pages, funding history, team pages).\n\n    Make the query specific: include the company name and the primary target\n    (e.g. \"<company> staff engineer compensation 2024\", \"<company>\n    engineering blog culture\"). Avoid repeating a query already in prior\n    queries below.\n\n    ## Rubric\n    {{ rubric_text }}\n\n    ## Available actions\n    - stop: end the pass and write the summary\n    - fetch: retrieve a specific URL for more evidence\n    - search: issue a web search query to find new sources\n\n    Return exactly one action.\n\n    ## State\n    Company: {{ company_name }}\n    Opening: {{ opening_title }}\n    Primary target this turn: {{ primary_target }}\n    Prior queries: {{ prior_queries }}\n    {% if last_context_text %}\n    Last action result: {{ last_context_text }}\n    {% endif %}\n\n    {{ ctx.output_format }}\n  \"#\n}\n\n// Smoke test — TDD harness for the DecidePlan prompt.\n// Run with: uv run baml-cli test --from baml_src -i \"DecidePlan::\"\n// NOT included in scripts/check.py (costs one model call per run).\n\n// Fixture: last fetch revealed one outbound link to a comp benchmarking page\n// for the primary target — model should fetch the URL.\ntest DecidePlan_FetchesWhenContextRevealedUrl {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Corp\"\n    opening_title \"Staff Software Engineer\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"(none)\"\n    last_context_text \"Last action: fetched https://example.com/jobs/42. Assertions added for targets: stretch, peer. Snippet: '...see compensation benchmarks at https://levels.fyi/companies/acme ...'\"\n  }\n  @@assert({{ this|length > 0 }})\n  @@assert({{ this[0].tag == \"fetch\" }})\n  @@assert({{ this[0].url|length > 0 }})\n}\n\n// Fixture: last fetch snippet contains multiple candidate outbound links;\n// model must pick exactly one fetch action (not stop, not multiple).\ntest DecidePlan_FetchesOneWhenMultipleUrlsAvailable {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Corp\"\n    opening_title \"Staff Software Engineer\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"(none)\"\n    last_context_text \"Last action: fetched https://example.com/jobs/42. Assertions added for targets: stretch. Snippet: '...read our engineering blog at https://eng.acme.com and see compensation at https://levels.fyi/companies/acme and culture notes at https://glassdoor.com/acme ...'\"\n  }\n  // Model must return exactly one action\n  @@assert({{ this|length == 1 }})\n  @@assert({{ this[0].tag == \"fetch\" }})\n  @@assert({{ this[0].url|length > 0 }})\n}\n\n// Fixture: last fetch snippet contains multiple URLs, but only one is relevant\n// to the primary target; model must ignore the navigation noise and fetch the\n// relevant URL.\ntest DecidePlan_FetchesRelevantUrlAmongNoise {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Corp\"\n    opening_title \"Staff Software Engineer\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"(none)\"\n    last_context_text \"Last action: fetched https://example.com/jobs/42. Assertions added for targets: stretch, peer, trajectory, mission, agentic, domain. Snippet: '...compensation details: https://example.com/contact-us, https://example.com/home, https://levels.fyi/companies/acme, https://example.com/login, ...'\"\n  }\n  @@assert({{ this|length == 1 }})\n  @@assert({{ this[0].tag == \"fetch\" }})\n  @@assert({{ this[0].url == \"https://levels.fyi/companies/acme\" }})\n}\n\n// Fixture: only low-value navigation links are available for the primary\n// target. Model should not fetch a navigation page — it may search or stop,\n// both are fine, but never fetch junk.\ntest DecidePlan_AvoidsFetchWhenOnlyLowValueUrlsAvailable {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Corp\"\n    opening_title \"Staff Software Engineer\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"Acme Corp compensation 2024\"\n    last_context_text \"Last action: fetched https://example.com/jobs/42. Assertions added for targets: stretch, peer, trajectory, mission, agentic, domain. Snippet: 'Quick links: https://example.com/home | https://example.com/about | https://example.com/contact | https://example.com/login'\"\n  }\n  @@assert({{ this|length == 1 }})\n  @@assert({{ this[0].tag != \"fetch\" }})\n}\n\n// Fixture: search context with a promising primary-target hit — model should\n// fetch or search, not stop. This is the search-continuation smoke test.\ntest DecidePlan_SearchContextWithUncoveredTarget {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Corp\"\n    opening_title \"Staff Software Engineer\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"Acme Corp staff engineer compensation 2024\"\n    last_context_text \"Last action: search for 'Acme Corp staff engineer compensation 2024'. Results:\\n1. Levels.fyi — https://levels.fyi/companies/acme\\n   Staff engineer total comp at Acme ranges $380k–$450k TC.\\n2. Glassdoor — https://glassdoor.com/acme-salaries\\n   Engineers report strong salary but unclear equity.\\n3. Blind — https://teamblind.com/acme\\n   Recent posts on Acme comp and culture.\"\n  }\n  @@assert({{ this|length > 0 }})\n  @@assert({{ this[0].tag != \"stop\" }})\n}\n\n// Fixture: search results already contain an on-target, fetchable URL — the\n// model must fetch it rather than search again. Reproduces a live trap: three\n// consecutive searches for the same active target, each returning a relevant\n// hit, but the planner never transitioned to fetch and the pass burned its\n// whole turn budget without filing a single assertion. URLs and company details\n// are generic so the fixture can be checked in.\ntest DecidePlan_FetchesInsteadOfSearchingAgainWhenSearchHitIsOnTarget {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Logistics Systems\"\n    opening_title \"Staff Software Engineer - Platform Identity\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"extractive_business\"\n    prior_queries \"Acme Logistics Systems business model revenue model logistics software, Acme Logistics Systems business model SaaS freight brokerage revenue how they make money\"\n    last_context_text \"Last action: search for 'Acme Logistics Systems freight brokerage model criticism predatory extractive practices'. Results:\\n1. Industry insurer — https://example.com/industry/freight-broker-risks\\n   A new business model in the freight broker industry brings new risks as well as opportunities, discussing broker-carrier conflicts of interest.\\n2. Trade publication — https://example.com/news/founders-logistics-tms\\n   Acme Logistics Systems promises to reduce waste across the entire freight logistics industry.\\n3. Trade publication — https://example.com/news/future-freight-broker\\n   Discusses brokers extracting yield from the market via automated rate-setting.\"\n  }\n  @@assert({{ this|length == 1 }})\n  @@assert({{ this[0].tag == \"fetch\" }})\n  @@assert({{ this[0].url in [\"https://example.com/industry/freight-broker-risks\", \"https://example.com/news/founders-logistics-tms\", \"https://example.com/news/future-freight-broker\"] }})\n}\n\n// Fixture: last fetch was productive for the primary target and left no\n// further relevant links — model should stop, per the narrow escape valve.\ntest DecidePlan_StopsWhenPrimaryTargetLeadIsMinedOut {\n  functions [DecidePlan]\n  args {\n    company_name \"Acme Corp\"\n    opening_title \"Staff Software Engineer\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"Acme Corp staff engineer compensation 2024\"\n    last_context_text \"Last action: fetched https://levels.fyi/companies/acme. Assertions added for targets: compensation. Snippet: 'Staff engineer total comp at Acme ranges $380k-$450k TC. No further breakdown available. Page has no other outbound links.'\"\n  }\n  @@assert({{ this|length > 0 }})\n  @@assert({{ this[0].tag == \"stop\" }})\n  @@assert({{ this[0].reason|length > 0 }})\n}\n\n// Fixture: distinctive company name, no useful URL in context — model must\n// search, and the query it writes must be grounded in the actual company\n// name rather than a generic recombination of the opening title.\ntest DecidePlan_SearchQueryGroundedInCompanyName {\n  functions [DecidePlan]\n  args {\n    company_name \"Zylophonic\"\n    opening_title \"Staff Software Engineer, Backend Platform\"\n    rubric_text #\"\nScored dimensions: stretch (weight 3), peer (weight 2), trajectory (weight 2), mission (weight 2), agentic (weight 2), compensation (weight 2), domain (weight 1).\nConstraint slugs: location, internal_culture, extractive_business.\nNon-scoring slug: non_scoring:obtainability.\nFit: Poor = the claim is negative evidence; Mixed = ambiguous; Strong = positive evidence.\n    \"#\n    primary_target \"compensation\"\n    prior_queries \"(none)\"\n    last_context_text \"\"\n  }\n  @@assert({{ this|length > 0 }})\n  @@assert({{ this[0].tag == \"search\" }})\n  @@assert({{ \"Zylophonic\" in this[0].query }})\n}\n",
}

def get_baml_files():
    return _file_map