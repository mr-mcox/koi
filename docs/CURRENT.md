Display leaf for the crossing-probability signal: `attention-allocation.md` split computation
(done) from display (not yet oriented). The signal is computed, JSON-exposed, and validated
via `/contested`; nothing renders it in `queue.html` yet. Start in `phases/orienting.md` using
`docs/features/review-ux/attention-allocation.md` and its scouting doc as parents.
